<?xml version="1.0" encoding="UTF-8"?>
<project default="oclToJava" name="fr.enseeiht.ocl.OCLToJava">
    <property name="ECLIPSE_HOME" value="../../../../../../../../../Logiciels/modeling-2023-09/eclipse"/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="fr.enseeiht.ocl.OCLToJava.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/fr.enseeiht.ocl.OCLToJava/bin"/>
    </path>

    <path id="fr.enseeiht.ocl.OCLToJava.classpath">
        <path refid="fr.enseeiht.ocl.OCLToJava.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="fr.enseeiht.ocl.OCLToJava.libraryclasspath.alljars">
        <pathelement location="../fr.enseeiht.ocl.OCLToJava/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="fr.enseeiht.ocl.OCLToJava.classpath.alljars">
        <path refid="fr.enseeiht.ocl.OCLToJava.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="oclToJava">
        <java classname="fr.enseeiht.ocl.OCLToJava.main.OclToJava" classpathref="fr.enseeiht.ocl.OCLToJava.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="oclToJava.alljars">
        <java classname="fr.enseeiht.ocl.OCLToJava.main.OclToJava" classpathref="fr.enseeiht.ocl.OCLToJava.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
